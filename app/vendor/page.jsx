// app/vendor/page.jsx
// Vendor portal root — renders the dashboard directly (same pattern as
// /admin). Middleware redirects unauthenticated visitors to /vendor/login.
export { default } from './dashboard/page'
